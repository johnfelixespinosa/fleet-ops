#!/bin/bash
source "$HOME/.rvm/scripts/rvm"
rvm use 3.4.2 > /dev/null 2>&1
cd "$(dirname "$0")/.."

# Load environment from .env
if [ -f .env ]; then
  export $(grep -v '^#' .env | xargs)
fi

exec ruby bin/mcp_server
